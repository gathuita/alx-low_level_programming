#include <stdio.h>

/**
 * main - Print all alphabets in lowercase a-z except q and e
 * Result: Always 0 (Success)
 */
int main(void)
{
	char = lower;
	char = 'a';

	while (lower <= 'z' && !=q || !=e)
	{
		putchar(lower);
		lower++;
	}

	putchar('\n');

	return (0);
}
