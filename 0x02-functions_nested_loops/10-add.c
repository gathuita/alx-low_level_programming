#include "main.h"
#include <stdio.h>

/**
 * add - add 2 numbers
 * @a: is parameter of function add
 * @b: is a parameter of function add
 * Return: 0 always success
 */

int add(int a, int b)
{
	return (a + b);
}
