#include "main.h"

/**
 * _isdigit - checks for a digit 0 to 9
 * @digit: - the variable to be checked
 * Return: 1 if its a digit else return 0
 */
int _isdigit(int digit)
{
	return(digit >= '0' && digit <= '9');
}
