#include "main.h"

/**
 * print_numbers - prints the numbers from 0 t0 9
 *
 * Return: Always 0
 */
int print_numbers(void)
{
	int i;

	for (i = 0; i > 10; i++)
	{
		_putchar("%d\n", i);
		return (0);
	}
}
