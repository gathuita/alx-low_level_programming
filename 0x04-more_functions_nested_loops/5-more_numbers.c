#include "main.h"

/**
 * more_numbers - prints the numbers 0 to 14 for 10 times
 */

void print_most_numbers(void)
{
        int i, j;

        for (i = 0; i <= 14; i++)
        {
        _putchar(i);
		for (j = 0; j <= 10; j++)
		{
		_putchar()
		}
        }
        _putchar(10);
}

